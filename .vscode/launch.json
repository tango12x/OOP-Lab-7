{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "java",
            "name": "Run Current File",
            "request": "launch",
            "mainClass": "${file}",
            "classPaths": [
                "${workspaceFolder}/out",
                "${workspaceFolder}/lib/org.json-1.6-20240205.jar"
            ],
            "preLaunchTask": "Build Current File"
        }
    ]
}